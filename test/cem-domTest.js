/*global require, describe, it, expect*/
(function(){
    describe("cem-dom", function(){

        it("provides a $ function that gets a NodeList");
        it("takes CSS selectors and returns DOM elements");
        it("attaches an event via 'on'");
        it("removes an attached event via 'off'");
        it("attaches a one-off event via 'once'");

    });
})();

